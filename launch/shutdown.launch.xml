<launch>
    <arg name="hostname"/>
    <arg name="username" default="admin"/>
    <arg name="password"/>

    <node name="franka_shutdown"
          pkg="franka_lock_unlock"
          exec="shutdown"
          output="screen"
          args="$(var hostname) $(var username) $(var password) -w -r"
    />
</launch>
